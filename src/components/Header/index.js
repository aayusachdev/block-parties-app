import '../../styles/header.css';

/**
 * Component to display the appplication name on a sticky Header.
 */
export default function Header() {
  return (
    <header className="header">
    <h1 className="header-title">Berlin Block Party Finder</h1>
  </header>
  )
}